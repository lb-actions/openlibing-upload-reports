{
  "name": "openlibing-upload-reports",
  "version": "1.0.0",
  "description": "GitCode Actions plugin for uploading metadata to OpenLibing OBS bucket",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && ncc build dist/index.js --license licenses.txt",
    "package": "npm run build && zip -r openlibing-upload-reports-v$(node -p \"require('./package.json').version\").zip dist/ action.yml"
  },
  "keywords": [
    "gitcode",
    "actions",
    "openlibing",
    "metadata",
    "upload"
  ],
  "author": "openlibing",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "axios": "^1.6.0",
    "form-data": "^4.0.0",
    "mime-types": "^2.1.35"
  },
  "devDependencies": {
    "@types/mime-types": "^2.1.4",
    "@types/node": "^20.11.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.3.3"
  },
  "overrides": {
    "undici": "^6.28.0"
  }
}